//Implementation of While Loop
#include<stdio.h>
int main()
{
    int num, i;
    printf("Enter the Number: ");
    scanf("%d",&num);
    i = 1;
    while(i <= num)
    {
        printf("Every Iteration i Value is %d\n",i);
        i++;
    }
    return 0;
}
